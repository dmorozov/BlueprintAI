# Optional: override the MoGe checkpoint (default has metric scale + normal maps).
PHOTO_ASSIST_MODEL=Ruicheng/moge-2-vitl-normal

# Optional: force a device. Leave empty for auto (cuda when available, else cpu).
PHOTO_ASSIST_DEVICE=
