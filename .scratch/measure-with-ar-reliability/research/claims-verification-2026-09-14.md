# Claims verification (workflow, 2026-09-14) — 12 claims, 20 agents; per-claim verdicts and the completeness critic's gaps
CLAIMS

== C1: VERIFIED
  [primary] refuted=False conf=0.85
   corrections: Claim is substantively accurate. Precise restatement: google-ar/arcore-android-sdk #1762 (open, filed 2026-04-25, last activity 2026-07-23) reports that after Google Play Services for AR auto-updated to 1.54.x (1.54.260890093 from Play; 1.54.260890493 GitHub APK also fails), `Session.resume()` (Java; NDK `ArSession_resume`) throws FatalException with native log "Failed to register sensor to queue 0" on many Samsung phones/tablets on Android 16 / One UI 8.0–8.5 (Tab S10 FE, S25 FE, S24 FE, S24+, S26+, A14/A15/A16/A25/A35/A53/A55/A56 ...), including Google's unmodified hello_ar_java sample. Google engineer lferrer (leoferrer@google.com) acknowledged it on 2026-04-28 and on 2026-05-06 said the regression was identified and a hotfix APK was expected "in the next few days" with rollout a week later; no Google follow-up has appeared since, users' pings in June/July went unanswered, and no release note (1.54.0 or 1.56.0; there is no 1.55.0 GitHub release) mentions a fix. Whether the later 1.5
  [counter] refuted=False conf=0.85
   corrections: Core claim stands. Precision fixes: (a) Google's acknowledgement was 2026-04-28 and the hotfix statement exactly 2026-05-06 ("new APK ready in the next few days, with a full rollout ... a week after that"); no follow-up from Google since, issue still open with last comment 2026-07-23. (b) ">= 1.54" is only verified for 1.54.x builds (1.54.260890093 Play Store, 1.54.260890493 GitHub APK). Play Services for AR 1.55.x (Jul 2026) and 1.56.x (Aug–Sep 2026; current Play build 1.56.262080393, GitHub SDK 1.56.0 released 2026-09-04) have since shipped with no release note about this bug and no reports either confirming or denying the crash on affected FE/A-series devices; one Sep 2026 report shows stock HelloAR resuming on a Galaxy A54/One UI 8.5 with 1.56, but that model was never confirmed affected. Treat fix status on 1.55/1.56 as unverified. (c) Additional nuance: one reporter (Galaxy A56) found declaring HIGH_SAMPLING_RATE_SENSORS alone sufficient; the sideload workaround specifically used

== C2: REFUTED
  [primary] refuted=True conf=0.9
   corrections: google-ar/arcore-android-sdk #1784 (opened 2026-09-07, open, label bug, 1 self-comment, no Google reply) and #1785 (opened 2026-09-09, open, label bug, 0 comments) both report camera-to-IMU clock desynchronization ("Camera to IMU clock offset (…) exceeds threshold (5ms)", FEATURE_DSP_CAM_IMU_DESYNC) on Samsung Android 16 devices. #1784: Galaxy A54 5G (SM-A546B), One UI 8.5, Play Services for AR 1.56.262080393 — with Google's own hello_ar_kotlin the camera feed displays but VIO "repeatedly initializes, loses tracking, resets the map, and initializes again"; tracking "never becomes stable" and no planes are ever usable (logs: VIO_OUTPUT_NOT_TRACKING / "VisualInertialState is kNotTracking"); persists after factory reset and forced 30 FPS; the same APK works on a Galaxy A14; author links #1779 (Xiaomi, similar offset). #1785: Galaxy Tab S11 5G (SM-X736N), Android 16 — ARCore "frequently alternates between TRACKING and PAUSED" (so it does reach TRACKING); the defect is unstable/incorrect me
  [counter] refuted=True conf=0.7
   corrections: Accurate restatement: google-ar/arcore-android-sdk #1784 (opened 2026-09-07, Galaxy A54 SM-A546B, Android 16 / One UI 8.5, Play Services for AR 1.56.262080393) and #1785 (opened 2026-09-09, Galaxy Tab S11 SM-X736N, Android 16, ARCore version not stated) both report camera-to-IMU clock offsets exceeding ARCore's 5 ms threshold (18.8–34 ms on the A54; -54.8 ms on the Tab S11). On the A54, VIO repeatedly initializes, loses tracking ("VisualInertialState is kNotTracking. Reset the map"), and re-initializes, so plane detection never becomes usable. On the Tab S11, tracking "frequently alternates between TRACKING and PAUSED" with grossly wrong metric scale (0.30 m reported as 0.66–1.83 m) and depth. Both are open with no Google response as of 2026-09-14; no fix, point release, or newer SDK exists (v1.56.0, 2026-09-04, is latest). The same IMU-offset symptom is also reported on a Xiaomi Android 16 device with Play Services for AR 1.55/1.54 (#1779, 2026-08-04), so it is neither Samsung-exclusi

== C3: REFUTED
  [primary] refuted=True conf=0.85
   corrections: Accurate version: In ReactVision/virocore, SessionNative::resume()/update()/pause() and the SessionNative constructor (ArSession_create) discard the ArStatus returned by ARCore, and VROARSessionARCore::run() logs "AR session resumed" unconditionally, so a failed ArSession_resume (e.g. AR_ERROR_CAMERA_NOT_AVAILABLE / AR_ERROR_ILLEGAL_STATE) leaves the session silently paused and every ArSession_update silently returns AR_ERROR_SESSION_PAUSED. With the session paused the camera reports AR_TRACKING_STATE_PAUSED, which Viro maps to VROARTrackingState::Unavailable with reason None - identical to VROARScene's initial state - so the change-only guard in VROARScene::setTrackingState never fires again. JS does receive ONE forced onTrackingUpdated(TRACKING_UNAVAILABLE, NONE) at ARScene construction (from VROARScene::setDelegate's force=true call), but never a transition afterwards. The rendering statement is wrong: Viro attaches and draws the camera background surface before and regardless of th
  [counter] refuted=True conf=0.75
   corrections: Accurate restatement: virocore's SessionNative discards the ArStatus of ArSession_create/resume/update/setCameraTextureName (only configure() checks it); a failed resume is logged as "AR session resumed" and leaves the session paused; each frame then maps PAUSED/STOPPED -> Unavailable and NONE/BAD_STATE -> None, which equals VROARScene's initial state, so the change-only setTrackingState never fires AGAIN; renderFrame draws black (renderWaitingForTracking) unless state == Normal, whereas Google's hello_ar_c draws the camera background before checking tracking state. CORRECTION: it is wrong that JS "never" gets onTrackingUpdated — VROARScene::setDelegate forces one initial callback (Unavailable=1, None=1) at scene creation, delivered asynchronously on the main Looper after VRTARScene.setListener, so JS receives exactly one onTrackingUpdated(1,1) per mount and then silence (matches issue #499). Nothing has changed upstream as of 2026-09-14: no commits to the ARCore wrapper since 2026-06-

== C4: CONTESTED
  [primary] refuted=True conf=0.85
   corrections: @reactvision/react-viro 2.58.1 ships android/arcore_client/core-1.43.0.aar, which is Google's stock com.google.ar:core:1.43.0 AAR repackaged (2024-05-31) with all four jni/*/libarcore_sdk_c.so removed (plus stray .DS_Store files added); everything else is byte-identical. The renderer AAR (viro_renderer-release.aar) bundles libarcore_sdk_c.so for arm64-v8a, armeabi-v7a and x86_64 only (no x86 ABI), each byte-identical to Google's com.google.ar:core:1.51.0 stub; virocore at tag v2.58.1 declares `implementation 'com.google.ar:core:1.51.0'` in android/virocore, android/viroreact and android/sharedCode build.gradle, and its checked-in viroar jniLibs stubs are the 1.51.0 binaries (commit b02139ca, 2025-11-26). libviro_arcore.so has DT_NEEDED libarcore_sdk_c.so and imports 150 Ar* symbols (all @VERS_1.0), every one of which is a public arcore_c_api.h function and is exported with the same VERS_1.0 version node by the 1.43.0, 1.51.0 and 1.56.0 stubs, so a newer stub satisfies the dynamic linke
  [counter] refuted=False conf=0.85
   corrections: Core claim stands as of 2026-09-14. Two precision corrections: (1) "All shipped .so files are 16 KB page-aligned" should read "all 64-bit (arm64-v8a, x86_64) .so files are 16 KB-aligned (0x4000; libgvr/libgvr_audio 0x10000); the armeabi-v7a copies of the Bullet, freetype, gvr, libc++_shared and libarcore_sdk_c libs, plus arcore_client's armeabi-v7a/x86 libarcore_sdk_jni.so, are 4 KB (0x1000)-aligned" — irrelevant to the Android 15+ 64-bit requirement but literally not 'all'. (2) "swapping in a newer libarcore_sdk_c.so could load without a renderer rebuild" is true at the dynamic-linker level (1.56.0 shim keeps VERS_1.0, exports all 150 symbols viro needs, same NEEDED set, 16 KB-aligned), but the shim performs JNI lookups into com.google.ar.core.SessionCreateJniHelper from the Java classes.jar, and the 1.56.0 shim newly requires useProjectedApk(), which only 1.56.0's classes.jar has (absent in 1.43.0/1.51.0). Any shim newer than 1.51 must therefore be swapped together with the matching 

== C5: REFUTED
  [primary] refuted=True conf=0.65
   corrections: Accurate restatement: ARCore publishes no general client-SDK support window; the only ARCore deprecation policy page is the Cloud Anchor API policy at developers.google.com/ar/distribute/deprecation-policy (also at /ar/develop/cloud-anchors/cloud-anchor-deprecation-policy), and /ar/develop/deprecation-policy is a 404. A cut-off mechanism does exist (AR_UNAVAILABLE_SDK_TOO_OLD / UnavailableSdkTooOldException, "too old and no longer supported") but no schedule is documented. The Android release-notes TEXT for 1.44-1.56 drops no Android version; its only breaking changes are targetSdkVersion bumps (35 in 1.46, 36 in 1.50, 37 in 1.56). However, the shipped AARs silently raised the library minSdkVersion from 21 (1.44-1.50) to 23 (1.51-1.54) to 24 (1.56.0), which is an undocumented compatibility cut-off in that range, and the parallel iOS SDK 1.48.0 (2025-02-27) explicitly dropped deployment targets below iOS 13.0. There is no 1.55.0 release. ARCore 1.52.0 (2025-12-12) states "Verified this 

== C6: VERIFIED
  [primary] refuted=False conf=0.8
   corrections: Core claim stands; attribution details need fixing: (1) Chrome Status entry 5450241148977152 is the "WebXR AR Module" (shipped Chrome for Android M81); the WebXR Device API entry is 5680169905815552 (desktop/Android M79) and WebXR Hit-test is 4755348300759040 (Android M81). None of the three lists an Android WebView milestone. (2) The "not allowed in a WebView" statement is by Google's model-viewer maintainer Emmett Lalish (elalish@google.com) in google/model-viewer discussion #3096 on Jan 4 2022 ("Indeed, WebXR is not allowed in a WebView. However, it is enabled in the new and improved replacement: WebLayer."), not in the immersive-web/webxr issue tracker; he later (Sep 8 2022) noted WebLayer "hasn't become a public API yet", and WebLayer was removed from Chromium in Sept 2023. He repeated "WebXR is not yet supported in WebViews" on Apr 25 2023 (#4250). Chromium's own statements: blink-dev Intent to Ship WebXR Device API (Sep 30 2019) — available "on all platforms with the exception o

== C7: REFUTED
  [primary] refuted=True conf=0.6
   corrections: Accurate version: ARCore's Session.update() requires an OpenGL context in the default texture mode — the Java docs say it throws "MissingGlContextException if there is no OpenGL context available", and the C ArSession_update can return AR_ERROR_MISSING_GL_CONTEXT ("An operation required GL context but one was not available") or, separately, AR_ERROR_TEXTURE_NOT_SET (texture name not set before the first update). "Cannot run headless" is overstated: Google engineers stated on issue #259 that "You don't have to draw the camera image, but you need a context in which to update it... A purely offscreen context created directly with EGL would work", while a maintainer added "this isn't a usecase we explicitly support so YMMV" (#259) and "a GLSurfaceView or similar is required" (#1259). Issue #1375 ("ARCore without GLSurface.Renderer and in Foreground Services", opened 2022-04-28, labeled bug) is still open with no maintainer response; its only comment is a 2024-04-30 "did you find a solution
  [counter] refuted=True conf=0.75
   corrections: Changed: (a) "ARCore cannot run headless / Session.update() throws MissingGlContextException without a GL context" is only true for the default Config.TextureUpdateMode.BIND_TO_TEXTURE_EXTERNAL_OES. Since ARCore 1.37.0 (May 2023), configuring TextureUpdateMode.EXPOSE_HARDWARE_BUFFER (Android API 27+) makes setCameraTextureName(s) ignored and delivers frames as HardwareBuffers; Google's hello_ar_vulkan_c sample (pinned to ARCore 1.56.0) calls ArSession_update() from a plain SurfaceView thread with no EGL/GLES code at all, and Unity's ARCore plugin has shipped Vulkan-only rendering since 6.1.0-pre.1 (Aug 2024). A pose-only ARCore session with no app-side GL context is therefore feasible on API 27+ — you simply never call getHardwareBuffer(). (b) Issue #1375 is not literally "unanswered": it has one non-maintainer comment (2024-04-30) and zero Google responses; still open, labeled "bug". Unchanged: Shared Camera is Camera2-only and Google's shared-camera sample renders via GLSurfaceView +

== C8: VERIFIED
  [primary] refuted=False conf=0.9
   corrections: The claim is substantively accurate. Two precision refinements: (1) the module is a single cross-platform Expo view backed by ARKit on iOS AND ARCore on Android (README: "one custom Expo view backed by ARKit on iOS and ARCore on Android"), not an ARCore-only view; hit testing is exposed as `raycast(x, y)` (Kotlin calls `frame.hitTest`), and tracking state as the `onTrackingStateChange { state }` event. (2) The README states "Expo SDK 56 with the Expo Modules API" — exactly SDK 56 (devDependency expo ^56.0.7, peerDependency expo "*"); no primary source claims SDK 57 or "56+" support, so compatibility with this project's Expo 57 is unverified. Everything else is exact: @stewmore/expo-ar latest 0.2.2, MIT license (package.json/README; the LICENSE file text is Expo's create-expo-module boilerplate), ARCore `com.google.ar:core:1.48.0` pinned to SceneView 2.3.0, one npm maintainer/one human contributor (stewartmoreland), last release v0.2.2 on 2026-06-11 (3 months 3 days before 2026-09-14), 

== C9: VERIFIED
  [primary] refuted=False conf=0.93
   corrections: The claim is accurate as stated. Precise restatement with nuances: Issues #499 (opened 2026-07-21, Galaxy S24 / Android 16 / react-viro 2.57.4) and #513 (opened 2026-08-07, Galaxy A54 / react-viro 2.43.6) are both open with no fix or workaround; the only maintainer replies are from oliedis (org MEMBER) on 2026-08-24 — "unable to reproduce ... logcat outputs" on #499 and "very out of date version ... test using a more up to date development setup" on #513 (that reply cites "version 2.58.5", which does not exist; the latest published version is 2.58.1). Commit d6f5a99f (2026-07-09, "Temporary fix for black screen in a scene on android", replays onActivityPaused/onActivityResumed after GL init) is an ancestor of v2.57.4 and not of v2.57.3. It was not git-reverted but removed (commit b40db16a "remove blocking camera-refresh workaround", part of PR #505 "v2.57.5", merged 2026-07-27); the v2.57.5 notes attribute the removal to ~5 s ANRs and state that "the renderer handles camera-texture bin
  [counter] refuted=False conf=0.9
   corrections: None to the substance; the claim is accurate as of 2026-09-14. Minor precision notes: (1) the maintainer's #513 reply cites "version 2.58.5", a version that does not exist on npm or GitHub (max is 2.58.1) — the claim's "ask to upgrade" characterization is still correct. (2) PR #505 is not framed by the maintainers as a plain revert: the 2.57.5 notes say the workaround was removed because the renderer now "handles camera-texture binding natively" and the black-passthrough issue "stays fixed", but no corresponding virocore camera-binding change exists in v2.57.3...v2.57.5, and #499 was reported against 2.57.4 (which still contained the workaround), so neither the workaround nor its removal is a confirmed fix. (3) "Nothing on develop or release branches" should be read as "nothing addressing ARCore session/camera startup, black passthrough, or Android 16": an unreleased release/2.58.2 branch (Quest input + ViroController + web fixes, rebuilt renderer AAR, last commit 2026-09-10) and a dev

== C10: REFUTED
  [primary] refuted=True conf=0.7
   corrections: SceneView arsceneview 4.35.0 (Maven Central, 2026-09-11) is Compose-only (no View/Fragment classes in the AAR; migration guide: "fully Compose-native", "no Fragment API"), and its AAR metadata sets minCompileSdk=37; its POM pins kotlin-stdlib 2.4.10, ARCore 1.54.0 and compose-bom 2026.06.01 (built with AGP 9.4.0). The older 2.x line is not "the 2.3.0 line (ARCore 1.48)": it continued to 2.3.1, 2.3.2 and 2.3.3 (last release 2026-01-13), and 2.3.3 pins ARCore 1.52.0, kotlin-stdlib 2.2.21 and has no compileSdk floor (minCompileSdk=1); only 2.3.0 itself used ARCore 1.48.0. The project publishes no statement that the 2.x line is frozen; it has simply had no release since January 2026 while 4.x ships weekly. Expo SDK 57 (released 2026-06-30, RN 0.86) does use compileSdk/targetSdk 36 and Kotlin 2.1.20 by default (Expo fallback 2.0.21), with AGP 8.12.0 pinned by RN; because API 37 needs AGP >= 9.1.1 and Kotlin binaries are only forward-compatible one minor release, integrating SceneView 4.x in

== C11: VERIFIED
  [primary] refuted=False conf=0.85
   corrections: Claim holds on every checkable detail; one attribution nuance: the AbsRel 0.073 figure is the DA-V2 paper's Table 4 in-domain result for a ViT-S fine-tuned on NYU-D, not a figure reported for the released Hypersim-trained Metric-Indoor-Small checkpoint, which has no published accuracy numbers at all. Accurate restatement: "Depth Anything V2 Small (24.8M) metric variants: the paper reports in-domain (NYU-D fine-tuned) AbsRel 0.073 / δ1 0.961 for ViT-S; the released Metric-Indoor(Hypersim)-Small has no published metrics. Official releases are .pth / Transformers safetensors only; no official TFLite/LiteRT/ONNX export exists (only community Core ML / ONNX / Android for the relative model). Qualcomm AI Hub ships only DepthAnything_V2_Small = depth-anything/Depth-Anything-V2-Small-hf (tagged 'relative depth'), 24.7M params, 94.3 MB fp32, TFLite/ONNX/QNN_DLC. MoGe-2 ViT-S (moge-2-vits-normal, 35M) has an author-hosted ONNX (Ruicheng/moge-2-vits-normal-onnx, model.onnx 140,852,051 bytes ~ 141

== C12: VERIFIED
  [primary] refuted=False conf=0.8
   corrections: Substance holds, with two precision caveats: (a) ARCore's list names the device "Galaxy S22 Ultra 5G" with comment "Supports Depth API" — it does not list model codes, so "SM-S908U" is not literally on the page (the mapping SM-S908U = Galaxy S22 Ultra comes from Samsung/AT&T pages). (b) The Play Store listing shows "Version: Varies with device" and "Updated on Aug 21, 2026"; it never displays "1.56". That 1.56 (1.56.2620803xx) is the current release train is established instead by Play reviews dated Sep 1–7, 2026, APKMirror uploads Aug 21–Sep 4, 2026, the GitHub ARCore SDK v1.56.0 release (2026-09-04), Google Maven com.google.ar:core 1.56.0, and Google's emulator docs. Also note 1.55 has no GitHub/Maven SDK release (1.54.0 -> 1.56.0).
  [counter] refuted=False conf=0.9
   corrections: none of substance. Accurate restatement: Google's ARCore supported-devices page lists "Galaxy S22 Ultra 5G" (no model-number breakdown; SM-S908U is the US variant but Google does not enumerate variants) with "Supports Depth API". Google Play Services for AR 1.56 (build 1.56.262080393; Play Store "Updated on Aug 21, 2026") is the current Play Store release as of 2026-09-14, matching SDK 1.56.0 (Maven latest 1.56.0, lastUpdated 2026-08-31; GitHub release v1.56.0 published 2026-09-04). No 1.57 exists on Play, Maven, or GitHub.

GAPS
- (device-test) No failing-session logcat or camera-owner dump captured from the S22 Ultra
    why: Every root-cause hypothesis (H1 sensor-registration failure, H2 VIO desync, H3 camera handoff, H4 GL surface never created) is inferred from upstream reports; the research notes confirm the device's logcat ring buffer held no ARCore session. Viro discards ever
    close: Build the current tree, then per trial: `adb logcat -c; adb logcat -v time > ar-fail-<n>.log`, cold-launch, open Measure with AR, wait 25 s, run `adb shell dumpsys media.camera > cam-<n>.txt` while the view is black, press back, re-enter. Do 10 cold launches t
- (device-test) Google's hello_ar_kotlin control has not been run, and the local toolchain cannot build it unchanged
    why: This single test separates a device/Play-Services-for-AR failure (no engine choice will help) from a Viro-specific one. The sample pins compileSdk 37 with com.google.ar:core:1.56.0, but ~/Library/Android/sdk/platforms holds only android-36 and android-36.1, an
    close: Either (a) copy samples/hello_ar_kotlin into the scratchpad, set compileSdk/targetSdk 36 (the 1.56.0 AAR has no minCompileSdk floor), build with the installed SDK/AGP and `./gradlew installDebug`; or (b) `sdkmanager "platforms;android-37"` plus an AGP 9.x wrap
- (device-test) The #1762 sensor-warm-up workaround is untested on this device and has no delivery path in this repo
    why: The documented mitigation (declare android.permission.HIGH_SAMPLING_RATE_SENSORS and register no-op listeners for TYPE_GYROSCOPE_UNCALIBRATED and TYPE_ACCELEROMETER_UNCALIBRATED at SENSOR_DELAY_FASTEST before the session is created) needs no Viro change, but a
    close: Add a local Expo module (e.g. modules/ar-sensor-warmup with Kotlin start()/stop()) and a config plugin using withAndroidManifest to add the permission; call start() in ar-capture-screen before phase becomes 'ready' and stop() on unmount. A/B on the device: 10 
- (device-test) Play Services for AR 1.53 downgrade bisect not run; needs the user's consent on their daily phone
    why: The bisect used by #1762 reporters (sideload the GitHub release APK 1.53.260340193, then let 1.56 reinstall) is the definitive test of whether the installed 1.56 service is the trigger on this device, but it replaces a system Play component on the user's perso
    close: Ask the user. If accepted: download the 1.53 APK from the google-ar/arcore-android-sdk releases page, `adb install -r -d`, disable auto-update for Google Play Services for AR in the Play Store, run the Viro app and the hello_ar control 5x each, then re-enable 
- (research) Whether Play Services for AR 1.55/1.56 contains the #1762 hotfix is unknown
    why: Verification of C1 established that only 1.54.x failures are documented; Google promised a hotfix on 2026-05-06 and never followed up. The S22 Ultra runs 1.56.262080393 (installed 2026-08-29). If 1.56 is fixed, H1 drops and the plan should weight H2/H3/Viro-sp
    close: Re-read google-ar/arcore-android-sdk#1762 for comments after 2026-07-23; search the tracker for 'Failed to register sensor' or 'queue 0' with 1.55/1.56 or One UI 8.0 mentions; read #1769, #1778, #1761 for 1.56 status. Treat gap 4 (bisect) as the authoritative 
- (research) The 'sometimes worked' history is not correlated with the ARCore APK and One UI update dates
    why: The service updated to 1.56 on 2026-08-29 and the phone's security patch is 2026-08-05. If successful sessions only predate those dates, the failure is service/firmware-side regardless of engine; if it succeeded afterwards, intermittency points to sensor state
    close: Ask the user when Measure with AR last worked and whether it was on first entry or after a reference-photo step; capture `adb shell dumpsys package com.google.ar.core | grep -i 'install\|update'` and the phone's Settings > Software update history; note whether
- (decision) No decided mechanism to ship a Viro patch or ARCore-client swap under CNG, or to run it on EAS
    why: android/ and ios/ are gitignored (`/android`, `/ios` in .gitignore) and regenerated by `expo prebuild`; the fix options (replace project(':arcore_client') with com.google.ar:core:1.56.0, strip the 1.51 stub from viro_renderer-release.aar, or ship a rebuilt ren
    close: Choose one: (a) pnpm patch for gradle text plus a postinstall script that runs `zip -d` on the AAR; (b) a git fork of @reactvision/react-viro consumed as a git dependency with swapped AARs committed; (c) a local config plugin that rewrites the node_modules AAR
- (research) Cost and feasibility of rebuilding virocore to fix the swallowed ArStatus are unknown
    why: The only genuine Viro-side fix (check ArSession_create/resume/update status and emit onError/onTrackingUpdated) requires rebuilding libviro_arcore.so and libviro_renderer.so from ReactVision/virocore (Gradle + CMake, C++14; NDK 27.1.12297006 is installed local
    close: Time-box half a day: clone virocore at tag v2.58.1 into the scratchpad, run the Android Gradle build for the viroar/sharedCode modules, record whether it completes, wall time, and which artifacts must be copied into the AAR. If it builds, apply the minimal ArS
- (device-test) Runtime behaviour of swapping Viro's ARCore client (1.43 Java/JNI + 1.51 C stub) for stock 1.56.0 is unverified
    why: C4's counter-check shows the 1.56 C stub requires 1.56's classes.jar (useProjectedApk), so the whole arcore_client AAR must be replaced and the renderer's stub removed; dynamic-link compatibility is confirmed, but whether Viro's session still creates and track
    close: Once gap 7's mechanism exists, build and run on the device; check the ArCoreApk availability log, the `ARCore Version: SDK build name: 1.56` line, and whether onTrackingUpdated behaviour changes versus the gap 1 baseline; confirm the merged manifest keeps targ
- (device-test) GLSurfaceView-in-ExpoView behaviour under Fabric and expo-router is unproven for the owned module
    why: The recommended replacement hosts a GLSurfaceView (or TextureView + own EGL) inside an ExpoView. Z-order against the tap Pressable overlay and badges, surface create/destroy on push/pop/back, backgrounding, the Restart AR remount (key change), and expo-router'
    close: Spike modules/ar-trace (Kotlin, com.google.ar:core:1.56.0, hello_ar_kotlin BackgroundRenderer + ARCoreSessionLifecycleHelper, events onTrackingState/onError, AsyncFunction hitTest executed on the GL thread) behind a feature flag on the ar-capture route. Exerci
- (device-test) The AR-to-expo-camera camera handoff (H3) is unmeasured for both engines
    why: ar-capture-screen unmounts the AR scene to mount CameraView for reference photos and remounts AR afterwards; ARCore may hold the camera up to 10 s after pause, a resume while the camera is held fails silently in Viro, and an owned module would need an explicit
    close: Instrument each transition with `adb shell dumpsys media.camera` and CameraService logcat lines; measure time-to-release after AR unmount and after CameraView unmount; decide whether the owned module exposes a released promise from session.close() that the scr
- (decision) The app's tracking-quality and hit-result contract is Viro-specific and undefined for ARCore's state model
    why: /Users/dmorozov/Work/ITConsulting/BlueprintAI/src/lib/ar-capture.ts maps Viro TRACKING_NORMAL/LIMITED to confidence 1.0/0.7, and the screen takes results[0] of performARHitTestWithPoint regardless of hit type. ARCore exposes TRACKING/PAUSED/STOPPED plus Tracki
    close: Write the engine-agnostic JS contract (view props/events, hitTest result {position, kind, distance}), a mapping table (e.g. TRACKING with no reason -> normal; TRACKING with INSUFFICIENT_FEATURES/EXCESSIVE_MOTION -> limited; PAUSED/STOPPED -> unavailable), and 
- (decision) 'Reliably' has no acceptance criteria or ground-truth accuracy protocol
    why: No session-start success rate, time-to-TRACKING, or measurement error tolerance is defined, so no experiment can declare the plan done or choose between engines and configs; there is also no regression trigger for Play Services for AR or One UI updates, which 
    close: Set thresholds (e.g. >= 19/20 cold starts reach TRACKING within 15 s; a tape-measured 4-wall room with wall-length error <= 3 cm or 2 percent; corner re-tap repeatability <= 2 cm). Run the protocol on the S22 Ultra for the chosen engine with depth on and off, 
- (device-test) Depth API and plane-detection behaviour on the S22 Ultra is untested
    why: Corner taps on featureless white walls depend on DepthPoint hits (Google lists Depth API support for this device) or vertical-plane detection latency; the choice of Config.DepthMode.AUTOMATIC, PlaneFindingMode and InstantPlacement changes hit success and accur
    close: In the gap 10 spike (or the hello_ar control), log session.isDepthModeSupported(AUTOMATIC), time to first vertical plane in two rooms, and hit type/distance for 20 corner taps at 1-4 m with depth on versus off; choose module defaults from the data.
- (decision) iOS scope and engine parity are undecided
    why: The Viro plugin adds ViroReact and ViroKit pods to the Podfile, and the screen imports isARSupportedOnDevice/requestRequiredPermissions from Viro. An Android-only Kotlin module leaves iOS without AR unless Viro stays for iOS (two engines behind one contract) o
    close: Decide among: Android-only now (module platforms: ['android'], Platform.OS guard with an iOS fallback screen, remove the Viro plugin and verify `pod install` and an iOS build still succeed), keep Viro on iOS behind the same JS contract, or build the ARKit impl
- (decision) Upstream reporting steps are not part of the plan
    why: The ReactVision/viro#499 maintainer explicitly asked for logcat; Google's #1762 has no S22 Ultra data point; a small ArStatus-check patch to virocore could be accepted upstream and avoid a permanent fork. None of this is scheduled, and the diagnosis in the iss
    close: After gaps 1-2: post the Viro logcat and versions (react-viro 2.58.1, RN 0.86.3, Expo 57, SM-S908U, One UI 8.0, ARCore 1.56.262080393) on #499; if hello_ar fails, add the device to #1762 (or #1784 for desync) with an `adb bugreport`; open a virocore issue/PR f
- (decision) Fallback trigger and on-device-model criteria are undefined
    why: The goal allows an on-device model only as last resort, but nothing states which outcome triggers it; the self-hosted MoGe-2 photo-assist path already exists as a fallback, and adding react-native-fast-tflite or react-native-executorch to an Expo SDK 57 new-ar
    close: Decide the trigger (e.g. hello_ar fails on the device AND sensor warm-up does not fix it AND the 1.53 downgrade is unacceptable) and that the fallback is the existing photo-assist path rather than a new on-device model; if a model is still wanted, gate a separ
- (decision) Go/no-go on the @stewmore/expo-ar trial is undecided
    why: The alternatives report recommends a half-day trial, but the package is verified only for Expo SDK 56 (Kotlin 2.0.21 vs RN 0.86's 2.1.20), pins ARCore 1.48.0 via SceneView 2.3.0 with Android 16 behaviour unverified, and has one maintainer with no commits since
    close: Decide now: skip it if the owned module is the target, or run a scratch SDK 57 dev build with it, check that it compiles under Kotlin 2.1.20 and whether raycast returns hits on this device. Either way, mine its ExpoArView.kt for the ExpoView lifecycle pattern 
